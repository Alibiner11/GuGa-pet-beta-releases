# GuGa Pet Host macOS 内测版 v0.1.0-beta.1

这是 GuGa Pet Host 的首个公开 macOS 内测安装包，面向受邀的 Apple Silicon（M 系列芯片）测试用户。

## 下载与校验

| 文件 | 用途 | SHA-256 |
| --- | --- | --- |
| `GuGa Pet Host-0.1.0-arm64.dmg` | macOS 安装包 | `b80152b381638ecd854ba83973d494135e36d67fc65249d3e020b8d0d9cf336f` |
| `GuGa Pet Host-0.1.0-arm64.dmg.sha256` | 完整性校验文件 | 与上列 DMG 对应 |

在 macOS 终端中可运行：

```bash
shasum -a 256 "GuGa Pet Host-0.1.0-arm64.dmg"
```

输出应与上表一致。

## 安装与使用

1. 打开 DMG，将应用拖到“应用程序”目录。
2. 安装前阅读 DMG 根目录的“内测说明.md”。
3. 首次运行后进入“设置 → 模型与 API”，配置自己的 API Key、模型与第三方服务。
4. 需要音色克隆时，在“扩展与连接”选择 GPT-SoVITS V4（推荐）或 V2，应用会从 `v0.1.0` 引擎 Release 自动安装。

本构建未使用 Apple 开发者签名/公证。若 macOS 阻止首次启动，请确认下载来源后在 Finder 中右键应用并选择“打开”。

## 当前内容与状态

- 包含当前内测 Live2D 运行资源、语音/识别等本地能力资源；不包含 GPT-SoVITS 的大体积可选引擎。
- 上述语音、宠物和 Live2D 资源均是开发者在开发过程中用于功能验证的测试资源。内测包保留它们的唯一目的，是让受邀测试用户直接体验、复现和反馈；不构成商业素材授权、独立分发许可、再利用许可或其他用途的授权。
- 安装包经过无开发环境变量构建与资源审计：不含 `.env`、API Key、测试记录、日志、测试账号或本机用户数据。
- 这是持续完善中的内测版。模型兼容性、第三方服务适配、性能、稳定性与界面会在后续版本调整；请勿把它用于高风险或生产关键业务。

## 内测与资源边界

本版本仅限受邀测试、体验和反馈。测试用户须遵守内测规范，不能用于商业销售、收费服务、公开再分发、反向工程、单独提取/传播包内受保护资源，或将它们用于内测体验与反馈以外的用途。Live2D 和其他第三方资源仍受各自许可约束；使用或后续公开商业化前必须完成许可核验。详情见仓库的 [内测范围与资源说明](../INTERNAL_BETA_SCOPE.md)。

问题或权利反馈请通过本仓库 Issues 提交，并避免公开任何密钥、令牌、个人数据或未公开素材。
